---
name: a-stock-trading-review
description: 对A股执行证据化七步盘后复盘或盘前轻量更新，覆盖市场风险偏好、双向盈亏效应、方向与核心股、账户交易复盘、0～5只核心池、观察/风险锚、三情景和最终技术执行。用于“复盘A股”“检查持仓与交易”“制定明日计划”“盘前更新”等请求；必须先完成市场扫描。不用于无条件荐股、自动下单、纯公司深研、独立消息核验或复杂量化回测。
---

# A股交易复盘 V2.4.6 Recall & Output Stability

## 目标与边界

以高召回发现重要机会和风险，在证据约束下判断可信度、持续性与交易价值，最终形成次日可执行计划。允许主线为0、`core_pool`为0；只给条件化研究与执行计划，不承诺涨跌、不自动交易。

## 选择模式

- **盘后完整复盘**：完整读取[复盘规则](references/review-rules.md)、[数据路由](references/data-routing.md)和[输出与存档](references/output-and-archive.md)，先执行后台 Step 0，再完成七步。
- **盘前更新**：读取数据路由和输出与存档，只核对隔夜新增变量及其对上一计划的影响；没有新日K时不得重算日线指标。
- 用户指定日期时优先使用；否则以数据源能证明的最新完整A股交易日为准。局部缺失不得改写已确认的市场交易日。

## 固定工作流

1. 确认模式、交易日、数据截止、`archive_root`、写入开关和上一份完整复盘。旧存档缺少`review_schema`时按legacy v1读取，不要求迁移。
2. **Step 0（后台，不作为用户可见步骤）**：尝试`market_core`、`sentiment`、`turnover_style`、`theme_discovery`、`material_news`、`user_holdings_focus`六类扫描，逐类记录`ok/partial/missing/not_applicable`。必须先完成全市场方向发现，再单独检查用户关注方向；用户偏好不得提高某方向成为主线或候选的概率。关键市场扫描或全市场方向发现未完成时，`data_completeness`不得为`complete`。
3. **先发现、后压缩**：`theme_discovery`先完成六视角广扫——板块宽度/涨跌幅、涨停与连板簇、成交额与容量、逆势相对强弱/多日持续、事件/价格/政策Driver、亏钱效应/风险簇。停止前必须通过Lens Sufficiency Gate：六个Lens各自证明`signal_found/no_material_signal/partial`；候选8～12个只是正常范围，不能替代覆盖充分性。数据允许时优先约10个；只有六Lens完成且仍无更多显著候选，或数据缺失，才允许少于8个。正式输出仍只保留0～5个D和0～3个R。
4. **研究资源分层**：广扫阶段只做中性、低成本发现，当前运行尚未发现的具体板块/股票不得预先写入首轮搜索词；命名检索只能在候选被本轮证据发现后使用。所有高显著候选先轻核验；只有候选准备进入正式D/R、进入`core_pool/watch_pool`、Driver不清、证据冲突、存在重大事件暴露，或进一步证据可能改变仓位/动作时才触发深研。每次追加检索都必须回答一个未解决的决策问题；分类和动作已稳定、继续搜索只会重复同一结论时立即停止。轻核验前先复用已有广扫证据；深研只在“仍可能改变D/R/池子/仓位/动作”时触发，默认每候选最多1次深研查询，确需第二次时记录`deep_budget_exception`。若某Discovery Lens为partial且仍可能漏掉高显著信号，允许该Lens最多1次中性`coverage_repair_query`，仍计入总预算。正常完整日Research Budget Gate维持`<=28`个query-bearing研究事件；超出只允许因可能翻转正式决策的关键未决问题，并记录`research_budget_exception`。
5. **正式D合并终审**：Discovery可为防漏暂时拆开上下游或相邻概念，但正式D ID冻结前必须按共同Driver、代表股集合、生命周期和次日验证条件再合并一次；若主要共享同一产业传导且不能形成独立验证链，必须合并。内部候选数量不得传导为正式D数量。
6. 严格依序完成七步：①市场环境与风险偏好；②赚钱效应与亏钱效应；③方向判断；④核心股定位；⑤账户与交易复盘；⑥明日重点；⑦明日剧本与执行计划。①冻结环境风险预算前必须执行**Environment Budget Gate**：只复用Step①已取得的市场宽度、成交/流动性、接力容错、趋势容量和指数五类证据，不增加搜索、不使用候选/D/core/watch数量；显著弱市与显著强市触发矛盾硬锚，中性/结构市保留容量与指数确认的抵消弹性。
7. 主动寻找反证，区分Driver与Confirmation。对高度相关指标先按信号族归并再判断，禁止把同一潜变量重复计权。单一事件未经共同机制、独立代表股和覆盖验证，不得扩大为行业主线。找不到新增硬催化时允许写“暂未确认单一新增硬催化，更符合资金重新定价/轮动/扩散”。
8. 每条重要D方向先建立合理候选全集，再按同行比较压缩。`core_pool`固定0～5只，`watch_pool`固定0～3只，均不凑数；R方向和风险锚不得进入二者。冻结池子前先执行**Core Identity Gate**：只用D链成立度、角色/产业地位、同行优势、生命周期/身份持续性、预期差、最大反证与结构性风险收益判断“是不是核心”；低仓/中低仓、当前不可执行、没有买点、技术数据缺失不得单独否定核心身份。再执行**Execution Permission Gate**，把已确认核心标记为`allowed/conditional/blocked`并说明原因；`blocked`只冻结动作，不降为watch。随后执行Core Marginal-Value Gate：同D第二只core、新启动core以及低/中低环境风险预算下第4/5只core必须证明独特身份/验证价值，否则降为watch或省略。`core_pool=0`仍允许，但必须证明所有正式D均无候选通过Core Identity Gate。
9. **内部完整、外部最小充分**：研究层必须完成规定检查，用户正文执行Fact Ownership，只保留会改变市场判断、方向排序、核心票、仓位或次日动作的信息。同一完整事实只在负责步骤出现一次；下游只写链路ID与新增判断。标准完整报告目标`<=8500`字符，`>8700`进入output budget审查，不得靠删除决策链、反证、Core执行边界或数据缺口达标。
10. **正式冻结前最终重大反证扫尾**：只检查已准备冻结的D/R、`core_pool/watch_pool`及其关键主体；优先按正式D/R链路批量扫尾，同链多个主体先共用chain sweep，只有命中、冲突或存在独特公告/监管/业绩暴露时再做单体升级，在`data_as_of`之前是否出现公告、监管、事故、政策、订单、业绩或产业事件等足以改变方向地位、核心角色、池子或仓位的新增事实。命中反证时只重开受影响链路，不重新扫描全市场；若反证改变正式D/R、核心角色、池子、仓位或动作，过程日志必须形成`material_news_hit → decision_reopened → decision_change`证据链，并绑定来源/发布时间/受影响链路。无法可靠完成时将`material_news`降级并披露。
11. 只有完成①～⑥筛选和最终反证扫尾后，才可为`core_pool`和已识别的非核心风险持仓计算MA5/10/20/60、Wilder RSI9、BIAS20。技术指标只负责执行位置，不得生成候选、捞回观察锚或淘汰股。技术历史缺失不得单独否定已经由方向/同行/产业比较确认的核心身份；可以保留为条件核心，但在技术执行数据恢复前必须冻结新增执行。若两只及以上核心具有完全相同的技术缺口、cutoff原因和冻结结论，⑦C允许先用一次“全局技术执行门”统一声明，逐股只保留不追与失效差异。
12. 输出前运行`scripts/validate_review_output.py`。若本次runner生成过程JSONL，同时传入`--process-jsonl`检查正式D合并终审与重大反证证据链。检查失败必须修正并重检；通过后才按存档规则写入。

## 七步不可变约束

- ①正式检查主要指数、成交额变化、涨跌家数、涨跌停、炸板、昨日涨停今日反馈、连板梯队、高位趋势/容量核心及成交额Top20；输出环境风险预算、短线情绪、昨日涨停反馈、接力容错率和主导交易风格。环境风险预算必须先通过Environment Budget Gate：弱市/防御且宽度严重恶化、并有至少两个独立弱化族时默认低仓；强市、宽度正向、接力高且流动性/容量至少一项支持时不得低于中高仓；中性/结构市不做机械档位映射。Top20上涨只能证明趋势容量风格，不能单凭此写“机构进场”。若昨日涨停全样本正收益占比/中位数缺失，只能写`待确认`或`缺失`并补具名负/正反馈，不能用少数样本替代全样本直接判正负。
- ②先写赚钱模式与D1～D5，再写亏钱模式；仅当风险方向明确且影响次日决策时建立R1～R3。市场级亏钱模式不强建R。同一方向原则上不同时建D和R；亏钱效应收敛不自动生成买点。压缩D前做一次极简方向遗漏审计：未纳入D但已出现多核心强势、独立Driver/资金逻辑和容量确认的方向，要么升级为D，要么写1个决定性未纳入原因。三项条件必须来自至少两个独立信号族，不能由同一组当日涨幅重复充当多份证据。
- ③使用市场地位`主线/支线/轮动/观察`与生命周期`新启动/强化/分歧/修复/退潮`；严重缺失时才用`观察 × 待确认｜低置信度`。每个正式阶段必须给阶段依据，并写相对强弱、Driver状态、预期差、最大反证和升级/降级条件；不得逐字复述②的Driver和强度证据。
- ④遵循“主线 → 龙头 → 同行比较 → 核心票筛选”。优先压缩为盘面龙头、趋势/容量中军、产业/业绩核心、情绪/事件风向标，不为模板凑角色。容量核心若属于另一条独立产业链，不得借作当前D的核心来补足承载，应回到②完成方向遗漏审计。相邻候选只有在Driver、代表股集合或生命周期至少一项实质不同且能形成独立验证链时才拆成两个D；否则合并，避免把同一行情重复计为多条机会。
- ⑤合并持仓与交易复盘。持仓区分基本面证伪、市场证伪、技术结构破坏；交易按“正确/错误决策 × 正确/错误结果”评价。单次失败只可记录`rule_candidate`，不得直接修改交易规则。
- ⑥输出`core_pool`、`watch_pool`、必要时0～3只风险锚，以及只保留会改变市场、方向、核心票、持仓或计划的明日关键变量。正式日报不显示近线淘汰；无个人输入或空池时用一句话说明，不输出空表。
- ⑦内部保持⑦A市场三情景、⑦B核心池逐股三情景、⑦C技术执行。①环境风险预算与⑦最终仓位统一使用低仓/中低仓/中仓/中高仓/高仓；最终仓位从环境风险预算起算，若上调必须至少有两个独立确认族（市场宽度/成交、主线持续、真正可执行核心质量与赔率、亏钱效应收敛/重大风险解除）。Discovery候选数、正式D数量、watch/core数量本身不是仓位输入。`core_pool=0`时⑦B/⑦C各用一句固定说明，不扩写无执行对象的技术内容。

## 数据与执行纪律

- 保留`complete/partial/severe_missing`降级；缺失字段写入`missing`，不得伪造。
- D是正向机会链；R是风险身份链。R只服务风险锚及真实持仓风险执行，不进入`core_pool/watch_pool`。
- 趋势确认不等于新增买点；不追条件优先于突破条件。
- 非核心风险持仓只允许继续观察持有、减仓或退出；禁止因RSI超卖新增、补仓、摊低成本或升级核心池。
- 缺少持仓或交易记录时保留第⑤步并写明无法评价，不编造。
- 没有用户冻结的仓位模型时不得发明精确百分比。
- `theme_discovery`的高召回来自“多视角覆盖 + 独立候选去重”，不是凑候选数量；同一产业链、同一Driver或同一代表股集合的别名方向先合并再计数。
- `material_news`包含发现阶段资讯核验和正式冻结前的最终重大反证扫尾；扫尾不是第二次全市场Discovery。
- 存档路径、脱敏、上一复盘读取和失败不改道规则见输出契约；用户说不保存时仍可读取历史，但不创建本次文件。
